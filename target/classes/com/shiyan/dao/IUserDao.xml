<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.shiyan.dao.IUserDao">
    <select id="findAll" resultType="com.shiyan.domain.User">
        select * from user
    </select>

    <insert id="saveUser" parameterType="com.shiyan.domain.User">
        <selectKey keyProperty="id" keyColumn="id" resultType="java.lang.Integer" order="AFTER">
            select last_insert_id()
        </selectKey>
        insert into user(username,address,sex,birthday)values(#{username},#{address},#{sex},#{birthday})
    </insert>

    <update id="updateUser" parameterType="com.shiyan.domain.User">
        update user set username=#{username},address=#{address},sex=#{sex},birthday=#{birthday} where id=#{id}
    </update>

    <delete id="deleteUser" parameterType="java.lang.Integer">
        delete from user where id=#{uid}
    </delete>

    <select id="findById" parameterType="java.lang.Integer" resultType="com.shiyan.domain.User">
        select * from user where id=#{uid}
    </select>

    <select id="findByName" parameterType="java.lang.String" resultType="com.shiyan.domain.User">
        select * from user where username like #{username}
    </select>

    <select id="findTotal" resultType="java.lang.Integer">
        select count(id) from user
    </select>

    <select id="findUserByVo" parameterType="com.shiyan.domain.QueryVo" resultType="com.shiyan.domain.User">
        select * from user where username like #{user.username}
    </select>

    <select id="findByUser" parameterType="com.shiyan.domain.User" resultType="com.shiyan.domain.User">
        select * from user
        <where>
            <if test="username != null and username != ''">
                and username=#{username}
            </if>
            <if test="sex != null and sex != ''">
                and sex=#{sex}
            </if>
        </where>
    </select>

    <select id="findInIds" resultType="com.shiyan.domain.User" parameterType="com.shiyan.domain.QueryVo">
        select * from user
        <where>
           <if test="ids !=null and ids.size()>0">
                <foreach collection="ids" open="and id in(" close=")" item="uid" separator=",">
                    #{uid}
                </foreach>
           </if>
        </where>
    </select>
</mapper>